<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.MainActivity"
    android:orientation="vertical">

    <ListView
        android:id="@+id/listaContatos"
        android:layout_width="match_parent"
        android:layout_height="453dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="0dp"
        android:layout_marginBottom="58dp" />

    <Button
        android:id="@+id/novoContatoButton"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_alignParentRight="true"
        android:layout_alignParentBottom="true"
        android:layout_marginRight="16dp"
        android:layout_marginBottom="14dp"
        android:background="@drawable/floating_buttom"
        android:elevation="20dp"
        android:text="+"
        android:textColor="#fff"
        android:textSize="28dp" />

    <EditText
        android:id="@+id/buscarEditText"
        android:layout_width="199dp"
        android:layout_height="57dp"
        android:layout_below="@+id/listaContatos"
        android:layout_alignParentStart="true"
        android:layout_marginTop="-58dp"
        android:ems="10"
        android:inputType="textPersonName"
        android:hint="Busca Contato" />

    <Button
        android:id="@+id/buscarButton"
        android:layout_width="wrap_content"
        android:layout_height="65dp"
        android:layout_below="@+id/listaContatos"
        android:layout_marginTop="-66dp"
        android:layout_toEndOf="@+id/buscarEditText"
        android:text="Buscar" />

</RelativeLayout>